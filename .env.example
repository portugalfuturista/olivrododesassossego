# O Livro do Desassossego environment variables
# Copy this file to .env and fill in the real values.
# .env is NOT tracked by git.

# z-ai GLM / Claude Code API key — used by MCP servers in .aurelio/mcp_config.json
Z_AI_API_KEY=
